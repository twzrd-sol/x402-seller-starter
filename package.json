{
  "name": "x402-seller-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One-click x402 seller on Cloudflare Workers, settled through a trust-gating facilitator.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run typecheck && npm test && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "tsx scripts/smoke.mts",
    "smoke:contrast": "bash scripts/contrast.sh"
  },
  "dependencies": {
    "hono": "^4.13.0",
    "x402-hono": "^1.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "@hono/node-server": "^2.0.10",
    "@types/node": "^22.13.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.5",
    "wrangler": "^4.120.0"
  },
  "engines": {
    "node": ">=22"
  }
}
